# Concursos

Plataforma de preparação para concursos. Inclui Next.js, FastAPI, PostgreSQL com pgvector e autenticação JWT.

```bash
cp .env.example .env
docker compose up --build
```

Abra `http://localhost:3000`; a documentação da API fica em `http://localhost:8000/docs`.
